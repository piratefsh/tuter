Summary of changes:
Gemfile:
gem 'webrick'
gem 'debugger'


Useful commands for development...

$ heroku git:remote -a tuter
$ git push heroku master
 
$ heroku pg:reset DATABASE --confirm tuter 
$ heroku run rake db:setup

$ heroku logs

Using gem 'annotate' for easy commenting on models, routes, etc

Unit testing
Modify the data for test database in test/fixtures
$ rake test:units