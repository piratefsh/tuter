<h1>Listing day_availabilities</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @day_availabilities.each do |day_availability| %>
  <tr>
    <td><%= link_to 'Show', day_availability %></td>
    <td><%= link_to 'Edit', edit_day_availability_path(day_availability) %></td>
    <td><%= link_to 'Destroy', day_availability, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Day availability', new_day_availability_path %>
